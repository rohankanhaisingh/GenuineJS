_ _ _ _ _ _ _ _ _ _ 
 - - - - - - - - - - PROPERTIES YOU CAN USE:

Main properties:
	Radius
	Color
	MoveSpeed
	Spawn ('center' or 'random')
	Amount (This isn't very important)
Effects:
	IsRain
	RandomMove,
	Gravity:
		IsActive
		Bounce
		Force
	Trail:
		IsActive
		Radius
		Length
		Color
	Connect: 
		IsActive
		Width
		Radius
		Color
	HitCorners:
		IsActive
		Destroy
		Explode:
			IsActive
			Force
			Speed
			Color

MouseEvents:
	Settings:
		Radius

	Hover:
		IsActive
		Radius
        Color
        MoveSpeed 
        PopSpeed
		MoveToMouseDirection:
			IsActive

		PushFromMouseDirection:
			IsActive
		Connect:
			IsActive
			Width
			Color

	MouseDown:
		Button
		IsActive
		Radius
        Color
        MoveSpeed 
        PopSpeed
		MoveToMouseDirection:
			IsActive

		PushFromMouseDirection:
			IsActive
		Connect:
			IsActive
			Width
			Color
		Summon:
			IsActive
			UseDefaultProperties
			UseMousePosition
			Amount
        


_ _ _ _ _ _ _ _ _ _ 
 - - - - - - - - - - JAVASCRIPT USAGE

// Usage:

var PopcornJS = Popcorn({
	// Object properties here
});

// Start, stop, save or load functions are included
// ------------------------------------------------

// Start the particles

PopcornJS.Start(100); // 100 is the amount of particles


// Stop the particles

GenuineJS.Stop();


// Save the properties to a JSON file

GenuineJS.Save('MyParticlePropertiesFile');


// Load the properties from a file url or import a file

// Load a file from a url
GenuineJS.Load('libraries/GenuineJS/config.json', function() {
	console.log('Hey, the files and the particles has been loaded!');
});

// Import a file
GenuineJS.Load('open', {
	Success: function() {
		console.log('Yay, the file and the particles has been loaded!');
	},
	Failed: function() {
		console.log('Ah no, something wrong happened while loading the file... Do you want to try it again?');
	}
});